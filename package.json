{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:frontend": "PORT=${PORT:-5000} BASE_PATH=${BASE_PATH:-/} pnpm --filter @workspace/treasure-hunt run build",
    "build:api": "pnpm --filter @workspace/api-server run build",
    "build:production": "pnpm run typecheck && pnpm run build:frontend && pnpm run build:api",
    "start:production": "NODE_ENV=production PORT=${PORT:-5000} pnpm --filter @workspace/api-server run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
